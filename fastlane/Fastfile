default_platform(:ios)

platform :ios do

  lane :build_and_test do
    scan(
      project: './WBMAnalytics.xcodeproj',
      prelaunch_simulator: true,
      clean: true,
      only_testing: 'WBMAnalyticsTests'
    )
  end
end
